<nav class="navbar navbar-expand-lg">
  <div class="container-fluid">
    <a class="navbar-brand" href="#"><img src="{{ asset('build/img/logo/futur.png') }}" alt="" class="logo"></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      {% if is_granted('ROLE_ADMIN') %}
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="{{ path('app_home')}}">AdminPanel</a>
        </li>
      {% endif %}
      </ul>
      <form class="d-flex" role="search">
        {# <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search"> #}
        {% if not is_granted('ROLE_ADMIN') %}
        <a href="{{ path('app_login')}}" class="btn text-decoration-none showButton">Login</a>
        {% endif %}
        {% if is_granted('ROLE_ADMIN') %}
        <a href="{{ path('app_logout')}}" class="btn text-decoration-none showButton">Logout</a>
        {% endif %}
        {# <button class="btn btn-outline-success" type="text">Register</button> #}
      </form>
    </div>
  </div>
</nav>