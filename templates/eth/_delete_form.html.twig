<form method="post" action="{{ path('app_eth_delete', {'id': eth.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eth.id) }}"><br>
    <button class="btn btn-danger">Delete</button>
</form>
