{% extends 'base.html.twig' %}

{% block title %}Eth{% endblock %}

{% block body %}
    <h1>Eth</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ eth.id }}</td>
            </tr>
            <tr>
                <th>CurrentPrice</th>
                <td>{{ eth.currentPrice / 100 }} €</td>
            </tr>
        </tbody>
    </table>

    <button type="submit" class="btn editButton"><a href="{{ path('app_eth_index') }}">back to list</a></button>

    <button type="submit" class="btn showButton"><a href="{{ path('app_eth_edit', {'id': eth.id}) }}">edit</a></button>

    {{ include('eth/_delete_form.html.twig') }}
{% endblock %}
