<form method="post" action="{{ path('app_collection_nft_delete', {'id': collection_nft.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collection_nft.id) }}">
    <button class="btn">Delete</button>
</form>
