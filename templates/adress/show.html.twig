{% extends 'base.html.twig' %}

{% block title %}Adress{% endblock %}

{% block body %}
    <h1>Adress</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adress.id }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ adress.city }}</td>
            </tr>
            <tr>
                <th>ZipCode</th>
                <td>{{ adress.zipCode }}</td>
            </tr>
            <tr>
                <th>Street</th>
                <td>{{ adress.street }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ adress.country }}</td>
            </tr>
        </tbody>
    </table>

    <button type="button" class="btn editButton"><a href="{{ path('app_adress_index') }}">back to list</a></button>

    <button type="button" class="btn showButton"><a href="{{ path('app_adress_edit', {'id': adress.id}) }}">edit</a></button>

    {{ include('adress/_delete_form.html.twig') }}
{% endblock %}
